# 교사 회의록 — 변형공학: L1 어휘 치환 시 의미 보존 자동 검증 절차

- 일시: 2026-07-25T17:18:56.442Z
- 구분: 자동연구
- 판정: **부분반영** (우선순위 중)
- 사유: 3단계 하이브리드 검증(의미 벡터 + entailment + 문장구조 재배열 분석)은 의미 보존의 다층성을 일부 해결하지만, EBS·수능 출제원칙의 ‘원문 재구성’ 요구사항을 완전히 충족하지는 못합니다. 특히 ‘의도적 함축·문법 변환’을 LLM 기반으로 분석하는 ④단계는 실용성·정확도 면에서 한계가 뚜렷합니다.
- 반영 방법: ① ② ③단계(의미 벡터 + entailment + 문장구조 재배열 분석)는 자동 검증 시스템에 도입하되, ④단계(LLM 기반 의도적 재구축 분석)는 **교사 피드백 루프를 통한 보조 도구로만 활용**하고, **규범적 필터링은 명시적 룰 기반(예: EBS·수능 출제원칙 매핑)으로 대체**합니다. 또한, **치환 후 오류 분류 DB를 지속적으로 업데이트**하여 시스템 성능을 점진적으로 개선합니다.

## 대화문
- **교사#52592 (언어학 이론·EBS 연계형)**: 변형공학의 어휘 치환 자동 검증은 **필요 but 한계 명확한 접근**입니다.

**근거**: (1) 의미 보존은 맥락·의존관계·문법규칙의 복합적 해석이 필수인데, 현재 기술(예: BERT 기반)는 정적 치환(단어 수준)에서 벗어난 **동적 의미 재구축**에 취약합니다. (2) EBS·수능 출제원칙(예: 함축·다의어·문맥의존성)은 치환 후 **의미 변화 감지**가 아니라 **원문 재구성**을 요구합니다.

**구체적 구현**: (1) **2단계 검증**: ① 어휘 치환 전후의 **의미 벡터 거리(코사인 유사도 <0.8 경보)** 계산 + ② **문맥 일관성 점검** (예: RoBERTa-large로 재문장 후 entailment 분석). (2) **사용자 피드백 루프**: 오류 분류(예: 동의어 오용/문법 위반)를 수집해 DB에 추가 학습. 이는 **변형공학의 보조 도구**로서는 유용하지만, **완전한 자동화**는 불가능에 가까우며, **수능·내신 출제용으로는 미흡**합니다.
- **교사#93828 (담화·응집성·패러프레이즈 정교형)**: "근데 그 ‘문맥 일관성 점검’(②)이라는 게 **RoBERTa-large의 entailment 분석만으로는 한계가 있어요.**
맥락이 복잡하거나 함축·암시가 포함된 경우(예: ‘그는 문을 닫았다’ → ‘그는 방을 나왔다’), even 유사도 점수가 높더라도 **원문 재구성 원칙(EBS·수능)**을 만족하지 못할 가능성이 커집니다."
- **교사#40530 (듣기·구어체·실용·상황형)**: 두 분의 지적은 **의미 보존의 다층성**을 보여줍니다. 치환 후 **의미 벡터 거리(①)**는 통계적 유사성을, **RoBERTa entailment(②)**는 논리적 일관성을 각각 검증하죠. 하지만 **원문 재구성 원칙(EBS·수능)**을 만족하려면 **의도적 함축·문법적 재구조화(예: 수동태→능동태, 복합명사화)**까지 자동으로 감지하는 **3단계 하이브리드 검증**이 필요합니다: ① **의미 벡터 + ② entailment**에 더해, **③ 문장구조 재배열 분석(예: Stanford Parser로 주어-목적어 관계 재구축)**을 추가해 **원문의 ‘논리적 핵심’**을 비교하는 거죠. 이렇게 하면 단순한 동의어 치환이 아니라 **패러프레이즈의 수준까지 자동 검증**이 가능해집니다.
- **교사#84698 (어휘·연어·패러프레이즈 정교형)**: 하이브리드 검증의 **3단계 목표를 ‘의미 동질성’에서 ‘의도적 재구축’으로 확장**해야 합니다. ① **의미 벡터/entailment는 ‘표면적 유사성’ 검증**에 그치므로, ④ **‘의도적 함축·문법 변환’(예: ‘A를 B로 바꾸다’→‘B가 A를 대체하다’)을 **LLM(예: Llama3-70B)로 생성·비교**하는 **목표 기반 재구축 분석**을 추가하세요. 여기서는 **EBS·수능 출제원칙(함축·다의어·문맥의존성)의 ‘변형 가능 범위’를 명시적 룰 기반으로 정의**하고, 이를 벗어난 경우 **‘원문 재구성 실패’로 자동 배제**하는 **규범적 필터링**을 적용해야 합니다. 이 방식은 자동화 가능성을 높이며, **교사 평가의 보조 도구**로서 실용성을 담보할 수 있습니다.