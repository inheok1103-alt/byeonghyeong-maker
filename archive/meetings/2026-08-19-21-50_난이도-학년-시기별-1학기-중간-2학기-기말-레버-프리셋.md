# 교사 회의록 — 난이도: 학년·시기별(1학기 중간~2학기 기말) 레버 프리셋

- 일시: 2026-08-19T21:50:40.731Z
- 구분: 자동연구
- 판정: **부분반영** (우선순위 상)
- 사유: 프리셋 구조의 단순화(2단계→계층형 모듈화)와 EBS 필터의 선택적 강화(80% 준수+20% 유연성)는 시스템 복잡성을 낮추며 창의·도전 문항의 배제를 방지하는 데 효과적이다. 난이도 코드의 객관성 제고(기능+난이도+맥락)는 채점 일관성을 높이는 데 기여할 수 있다.
- 반영 방법: [object Object]

## 대화문
- **교사#28114 (교육과정·성취기준·EBS 연계형)**: **찬성**
레버 프리셋은 학생 눈높이·출제 패턴·시스템 효율화를 동시에 고려한 '안전한 최적화'로, **학년·시기별 난이도 균형을 유지**하며 **시스템 메시지 불일치(불필요한 커뮤니케이션 오류)를 원천 차단**할 수 있습니다.

**구체적 구현:**
1) **학년별 3단계(기본·표준·고난도) 프리셋** → 1학기 중간(기본·표준), 2학기 기말(표준·고난도)로 분배
2) **EBS 연계 강제 필터** → 각 프리셋에 `EBS 핵심 문항 ID` 태그 부여(예: `EBS-1-2-3` → 1학년 1학기 2단원 3번)
3) **성취기준 자동 매핑** → 각 문항에 `[R/W/S/A]` 난이도+기능(Reading/Writing/Speaking/Analyzing) 코드 삽입.
- **교사#69350 (어휘·연어·서술형 채점 정밀형)**: **우려사항:**
1) **과도한 프리셋 분화로 인한 시스템 복잡성 증가** → 학년·시기별로 3단계(기본·표준·고난도) 프리셋을 나누면, 총 6단계(1중·1기·2중·2기 × 3단계)가 된다. 이 경우 **프리셋 간 연계성·일관성이 떨어지거나**, 오히려 **시스템 유지보수 비용(예: 프리셋별 테스트 케이스 관리, 업데이트 주기)**이 기하급수적으로 증가할 위험이 있다.

2) **EBS 필터 강제가 '창의·도전적' 성향과 충돌** → EBS 핵심 문항을 무조건 필터링하면, **학생의 사고력·응용력을 요구하는 '도전적' 문항이 배제될 우려**가 있다. 특히 **고난도 프리셋에서 표준화된 EBS 문항만 사용하면, '표준화된 답안'Centre of Excellence(COE) 위주로 훈련**될 뿐, **실제 수능·내신에서 요구하는 '창의적 문제해결력'이 저하**될 수 있다.

3) **난이도 코드 `[R/W/S/A]`의 객관성 부족** → 난이도(R/W/S/A)는 주관적 판단(예:Writing 난이도 판단 기준이 모호할 수 있음)으로 인해, **채점자·시스템 간 일관성이 깨질 위험**이 있다. 특히 **Speaking(A) 난이도 표기는 발음·유창성 등 비언어적 요소까지 고려해야 하므로, 자동화 시스템에서 구현하기 어렵다**.
- **교사#16052 (언어학 이론·실용·상황형)**: **절충안:**
1) **2단계 프레임 전환** → **학년·시기별 "기본/확장" 프리셋**으로 단순화(예: 1중/1기: 기본+확장, 2중/2기: 확장+도전). 복잡성은 낮추고, 고난도는 `확장` 프리셋 내 `난이도 태그([R1~R3/W1~W3/S1~S2/A1~A2])`로 세분화.
2) **EBS 필터는 "80% 준수 + 20% 유연성"** → 핵심 문항은 태그로 자동 포함하되, "창의·도전" 문항은 `확장 프리셋`에서만 선별적 포함(예: `EBS+창의-4-1-5`).
3) **난이도 코드는 "기능+난이도+맥락"으로 재정립** → `[R/W/S/A]_难度(1~3)_맥락(예:지문길이/주제복잡도)`로 객관화(예: `W2_길이500자_주제비교`). Speaking은 발음·유창성 대신 **대답 길이·구조 복잡도**로 대체.
- **교사#60220 (화용·함의·패러프레이즈 정교형)**: 1. **프리셋 구조의 "계층형 모듈화" 제안**
   - 학년·시기(1중/1기/2중/2기)를 **상위 레벨**로, 각 레벨에서 **기본/표준/도전(3단계)**을 **하위 모듈**로 분리하되, 도전 단계는 `확장 프리셋` 내 **맥락 기반 난이도 태그([R/W/S/A]_난이도_맥락)**로만 구성해 시스템 복잡성을 낮추고(예: `2기_도전 = [R3_길이800자_추론, W2_600자_비교분석]`), 프리셋 간 일관성은 상위 레벨에서만 강제한다.

2. **EBS 필터의 "선택적 강화"**
   - 핵심 문항(80%)은 `표준` 프리셋에 **자동 포함**하되, 창의·도전 문항은 `도전` 프리셋에서 **맥락 태그(예: `EBS+창의_4-2-1`)**로만 관리하여, EBS 연계성은 유지하되 **응용력 요구 문항의 유연성도 확보**한다.

3. **Speaking 난이도 표준화**
   - Speaking 난이도는 **구조 복잡도(예: 3문장 이상 답변, 연결어 사용)**로만 대체하고, **자동 채점 시스템**에서는 **답변 길이·구조 일관성**만 평가하도록 제한해 객관성을 높인다(예: `S2_답변4문장_연결어2개`).