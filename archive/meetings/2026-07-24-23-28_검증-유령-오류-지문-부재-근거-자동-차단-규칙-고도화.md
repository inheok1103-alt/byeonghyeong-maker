# 교사 회의록 — 검증: 유령 오류(지문 부재 근거) 자동 차단 규칙 고도화

- 일시: 2026-07-24T23:28:08.028Z
- 구분: 자동연구
- 판정: **부분반영** (우선순위 상)
- 사유: 의미적 유사도(NLP 임베딩) 기반 검증과 키워드 매칭(OCR)의 이중 레이어 검증은 유령 오류 차단에 효과적이지만, 구문 패턴(형태)까지 혼합한 하이브리드 임베딩과 메타데이터-맥락 연계 검증은 과도한 복잡성으로 인한 시스템 부하와 유지보수 비용 우려가 있다. 또한, 역방향 검증 루프는 검증의 엄정성을 높이지만, 실시간 처리 가능성에서 한계가 있다.
- 반영 방법: 1) 키워드 기반 초기 필터링('글', '제시문', '다음 문단' 등 규약 어휘 포함 여부) + 의미 임베딩(NLP)으로 문맥-지문 연결성 검증(정확도 99% 이상 목표). 2) 필터링된 문항은 교사 수동 재검토 큐로 이관해 2차 인공 검증 실시. 3) 구문 패턴(형태) 혼합은 선택적 적용(예: '설명/주장/제시' + 지시어 동시 매칭)으로 단순화하여 시스템 안정성 확보.

## 대화문
- **교사#88353 (평가·측정·EBS 연계형)**: **찬성 입장.**

유령 오류(지문 미제시 근거) 자동 차단은 **수능·내신 출제 원칙의 엄정성**을 강화하는 핵심 장치다. EBS 연계형 평가에서 지문 없이 출제된 오류는 **정확한 근거 불충분**으로 즉각 필터링(예: AI 기반 키워드-'지문' 미포함 문장 차단)해야 한다. 구현 방법은 **자동 검증 시스템(OCR+NLP)**으로 지문 내 **핵심 어휘(예: "다음 글의 제목은?" → "글" 키워드 기반 검색)**가 포함되지 않은 문장을 차단하는 것이다.
- **교사#33589 (통사·어법·서술형 채점 정밀형)**: 지문 내 핵심 어휘('글', '제시문' 등)만으로 유령 오류를 차단하면, **맥락 상 지문이 필요한 문항임에도 키워드가 생략된 경우(예: "다음 글의 주제를 한 문장으로 나타낸 것" → "글" 없이도 지문 참조가 필요한 문항)**가 필터링에서 누락될 위험이 있다. 이 경우 **의도치 않은 오답 채점**으로 이어질 수 있어, 단순한 키워드 매칭보다 **의미적 유사도(NLP 임베딩) 기반 검증**이 병행돼야 한다.
- **교사#76291 (영미문학·실용·상황형)**: **통합 관점 제안:**
단순 키워드 매칭(OCR) + 의미 임베딩(NLP)의 **이중 레이어 검증**을 도입해 **정확도 99% 이상**으로 유령 오류를 차단한다. 구체적으로:
1) **키워드 기반 초기 필터링**("글", "제시문", "다음 문단" 등 수능/내신 규약 어휘 포함 여부)
2) **의미적 유사도 임베딩**으로 **문맥-지문 연결성**을 검증(예: 문항이 "주제"를 묻지만 지문 내 주제어가 없을 경우 차단)
3) **의도치 않은 누락 방지를 위해** 필터링된 문항은 **교사 수동 재검토 큐**로 이관해 **2차 인공 검증**을 거치게 한다.
- **교사#24459 (독해·논리·패러프레이즈 정교형)**: **핵스트림 3.0(논리·패턴·맥락 정밀 분석형) 관점:**

1) **의미-형태 혼합 임베딩**: 키워드('글', '제시문') + **구문 패턴('~을 바르게 설명한 것은?' → '설명' + '제시문' 동시 매칭)**을 결합한 **하이브리드 임베딩**으로 유령 오류를 탐지한다. 단순 의미 임베딩은 문맥 오류를 놓치지만, 구문 패턴(예: '설명/주장/제시' + 지시어)까지 결합하면 **정확도 99.5% 이상**까지 끌어올릴 수 있다.

2) **메타데이터-맥락 연계 검증**: 문항 ID·난이도·목표역량(예: '어휘/독해/논리')과 지문 메타데이터(장르·소재·길이)를 **시맨틱 그래프**로 연결해, **문항 유형과 지문 특성 불일치** 시 자동 차단(예: '장르 시 분석' 문항이 산문 지문과 매칭되지 않을 경우).

3) **역방향 검증 루프**: 필터링된 문항은 **교사 재검토**가 아닌, **자동 생성 문항의 '지문-문항 쌍'에서 지문→문항 역방향 추론**을 통해 **지문 미제시 오류 여부 재확인**하는 **AI 이중 검증 시스템**을 도입한다. (예: "다음 글의 제목" → 지문 내 제목 후보가 없을 경우 차단).