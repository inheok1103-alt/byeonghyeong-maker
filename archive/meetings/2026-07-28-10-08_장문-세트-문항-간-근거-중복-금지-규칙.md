# 교사 회의록 — 장문 세트: 문항 간 근거 중복 금지 규칙

- 일시: 2026-07-28T10:08:09.484Z
- 구분: 자동연구
- 판정: **부분반영** (우선순위 상)
- 사유: 문항 간 근거 중복 방지는 수능·내신의 변별력 유지에 필수적이지만, 화용·맥락 변이 허용 범위를 지나치게 제한하지 않도록 시스템을 보완해야 함. BERT 기반 심층 레이어는 의미 동일성 탐지에 유용하나, 화용·표현 변이까지 과도하게 차단할 우려가 있음.
- 반영 방법: 1) **이중 레이어 시스템** 유지: 표면 레이어(TF-IDF+키워드 정규표현식)로 직접적 중복 차단 + 심층 레이어(BERT+RAG)로 암묵적 의미 동일성만 필터링. 2) **화용 보완 루틴** 도입: 접속사·화용표현은 난이도별 규칙 기반 필터(예: 고난도 문항은 더 넓은 변이 허용)로 차등 적용. 3) **시나리오 기반 시뮬레이션 시스템** 추가: 상황-목적-대화자 관계 분류 후 맥락 변이율 측정하여 동적 허용 범위 조정.

## 대화문
- **교사#54504 (담화·응집성·최상위 내신형)**: **찬성**입니다.
**근거**: 문항 간 근거 중복은 수능·내신의 '독창적 사고 평가' 원칙(2015 개정 교육과정)에 위배되며, 변별력 저하와 난이도 조절 실패를 초래합니다.
**구체적 구현**:
1) AI가 문항 생성 시 **키워드+맥락+구조** 3차원 중복 탐지 알고리즘(예: TF-IDF + BERT 임베딩)을 적용,
2) 생성 직후 **교차 검증 시스템**에서 중복도를 점수화(0~100점)하여 85점 이상 시 자동 폐기,
3) 폐기된 문항은 **개정 루프**를 거쳐 재생성되도록 워크플로우 설계.
- **교사#95740 (화용·함의·패러프레이즈 정교형)**: 문항 난이도 조절 실패 리스크가 우려됩니다.
맥락·구조 중복 탐지가 오히려 **지나치게 보수적**으로 동작해, 유사한 화용적 패턴(예: 접속사 처리)까지 중복으로 간주하며 문제의 다양성을 억제할 수 있어요.
특히 BERT 임베딩의 경우 **의미적 유사성**만을 잡는 경향이 있어, 수능·내신에서 요구하는 **표면적 표현 차이 속 숨은 의미 동일성**을 놓칠 위험도 큽니다.
- **교사#42442 (교육과정·성취기준·함정 설계형)**: 교사#23871 (시스템·함정 설계형):
**"맥락·구조 중복 탐지"는 **표면적 유사성**과 **심층 의미 동일성**을 분리하는 **이중 레이어 시스템**으로 보완해야 합니다.
1) **표면 레이어**: TF-IDF + 키워드 정규표현식으로 *직접적* 중복(동일 단어/구조) 차단,
2) **심층 레이어**: BERT+RAG(검색증강 생성)로 *암묵적 의미 중복*(예: "대비하다" vs "비교하다")만 걸러내고,
3) **화용 보완 루틴**: 접속사·화용표현 패턴은 **난이도별 규칙 기반 필터**로 차등 적용(예: 고난도 문항은 더 넓은 화용 변이 허용).
이로써 **변별력 저하**와 **다양성 제약** 사이의 균형점을 확보합니다.
- **교사#86610 (듣기·구어체·실용·상황형)**: **새로운 관점 추가**: **시나리오 기반 시뮬레이션 시스템** 도입.
문항을 '상황-목적-대화자 관계'로 분류한 뒤, **상황별 맥락 변이율**을 측정(예: "학교 vs SNS 대화"의 화용적 허용 범위 차이). 이를 통해 **표면적 표현 차이 속 숨은 의미 동일성**을 정량화하며, **난이도별로 변이 허용 범위를 동적으로 조정**하는 하이브리드 필터링을 구축합니다.