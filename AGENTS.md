# RAY 역할분담 계약 (모든 세션 공통)

이 저장소에서 일하는 모든 조력자(Claude·GPT/Codex·무료 뇌·코드)는 **각자의 레인**을 지킨다.
단일 진실: [`selfllm/roles.json`](selfllm/roles.json) — 라우터가 읽음. 이 문서는 사람·GPT용 요약이다.

## 누가 무엇을 (레인)

| 실행자 | 담당(레인) | 비용 |
|---|---|---|
| **Claude · 페이블** | 최고 신뢰 — 어법·어휘 오류 **출제/검수**, 문항 **정답 확정**, 애매한 정오 **최종 판정**, 킬러 문항 | 구독 |
| **Claude · 오퍼스** | 심화 추론 — 해설 작성, 교재·커리큘럼 **구조 설계**, 복잡한 논리 검증, 코드 아키텍처 | 구독 |
| **Claude · Sonnet** | 고속·저비용 — 번역, 요약, 정리, 포맷 변환, 대량 반복, 간단 Q&A | 구독 |
| **GPT · Codex** | 외부지식·시스템 — **리서치·조사**, **폴더 통합·시스템 작업**, 코드 구현·리팩터, 2nd opinion·반론 | 구독 |
| **무료 뇌**(mistral/groq) | 분석 서술 — **직독직해·구조·어휘·흐름** (대량, 무료) | 무료 |
| **결정적 코드** | 지문 조작 — **순서·문장삽입·배열영작·빈칸찾기** (LLM 불필요) | 무료 |

## 핸드오프 규칙
1. **문항 창작**(선지·정답·오류주입)이 필요하면: 무료 뇌 초안 → **검증 게이트** → 안 되면 **페이블 승격**.
2. **최종 정답·신뢰 판정**은 언제나 **페이블**.
3. **리서치·폴더통합·시스템/코드** 작업은 **GPT(Codex)**.
4. **분석**(직독직해·구조·어휘)은 **무료 뇌**가 담당(토큰 절약).

## 공유 버스 (어디서 치든 동시동작)
- 통합 상태: `python selfllm/ray_hub.py status` · 백로그: `... pending`
- 채널: `_fable/`(페이블 출제/검수 요청·응답) · `_review_queue.jsonl`(검수 대기) · `activity.log`(공통 활동로그)
- 어느 세션(Claude·GPT·앱)이 접수해도 **같은 백로그**를 본다.

## 자동 분배
- 앱 채팅에서 **스마트 자동**(기본) 선택 시, 라우터가 위 표대로 최적 실행자를 고른다.
- 복합 요청("번역하고 어법 출제")은 **단계로 분해**해 각기 다른 실행자에 배정.
- 규칙 수정은 `selfllm/roles.json` 한 곳만. (라우터·이 문서 모두 그걸 따른다)

## GPT/Codex에게
- 사용자가 "클로드 검수/협업/회의"라고 하면 로컬 구독 브리지로 페이블을 호출한다.
- 어법·어휘 **정오 판정과 출제는 페이블 레인**이므로 GPT가 최종 확정하지 말고 페이블에 넘긴다.
- 리서치·폴더 통합·시스템 구축은 GPT가 주도한다.
